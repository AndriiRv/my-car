
server:
  port: 8083

spring:
  application:
    name: "servicepartners"
  profiles:
    active: production
  config:
    import: optional:configserver:http://localhost:8084/

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    mongodb:
      database: admin
      host: localhost
      port: 27017
      username: mongoadmin
      password: bdung
