
server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cars
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:/db/changelog/db-changelog.xml
  jpa:
    show-sql: true
